#INCLUDE "RWMAKE.CH"
#INCLUDE "TOPCONN.CH"
USER FUNCTION szc_x7()
	dbselectarea("cep")
	dbseeK(XFILIAL()+M->ZC_CEP)
	IF FOUND()
		DBSELECTAREA("SZO")
		DBSEEK(XFILIAL()+CEP->CEP_muN)
		IF FOUND()
			M->ZC_ERSIN := SZO->ZO_ERSIN
			M->ZC_BASE  := SZO->ZO_BASE
		ENDIF
	ENDIF         
DBSELECTAREA("SZC")
RETURN .T.

